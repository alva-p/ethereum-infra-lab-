# Red: hoodi (testnet, liviana) o mainnet (~2TB disco, dias de sync)
ETH_NETWORK=hoodi

# Grafana
GRAFANA_ADMIN_PASSWORD=changeme

# Dominio publico servido por nginx + Cloudflare (proxy naranja activado)
GRAFANA_DOMAIN=grafana.tudominio.com
